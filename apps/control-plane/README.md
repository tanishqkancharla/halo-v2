# Control plane

Placeholder for Halo's control plane. The entry point prints a startup message.

Run from the repository root:

```sh
pnpm --filter @get-halo/control-plane dev
pnpm --filter @get-halo/control-plane start
```

`dev` watches for source changes. `start` runs once and exits.
