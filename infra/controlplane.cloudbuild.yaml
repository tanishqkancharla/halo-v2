steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - apps/control-plane/Dockerfile
      - --tag
      - ${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
timeout: 900s
