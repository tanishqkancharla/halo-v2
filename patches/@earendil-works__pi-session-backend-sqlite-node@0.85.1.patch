diff --git a/dist/sqlite/migrations/001_initial.sql b/dist/sqlite/migrations/001_initial.sql
index b621b598e4c0bd522ac559e26af6d8ca26164294..9a33d1e3c6cfc2400f4f311cd58c74d4c9238ecb 100644
--- a/dist/sqlite/migrations/001_initial.sql
+++ b/dist/sqlite/migrations/001_initial.sql
@@ -7,7 +7,7 @@
 -- projections/caches.
 
 CREATE TABLE IF NOT EXISTS sessions (
-	id TEXT PRIMARY KEY,
+	id TEXT PRIMARY KEY NOT NULL,
 	created_at INTEGER NOT NULL,
 	parent_session_id TEXT,
 	storage_version INTEGER NOT NULL,
@@ -15,7 +15,7 @@ CREATE TABLE IF NOT EXISTS sessions (
 	message_count INTEGER NOT NULL,
 	usage_payload TEXT NOT NULL,
 	next_seq INTEGER NOT NULL
-) WITHOUT ROWID;
+);
 
 CREATE TABLE IF NOT EXISTS entries (
 	session_id TEXT NOT NULL,
@@ -27,7 +27,7 @@ CREATE TABLE IF NOT EXISTS entries (
 	timestamp INTEGER NOT NULL,
 	payload TEXT NOT NULL,
 	PRIMARY KEY (session_id, id)
-) WITHOUT ROWID;
+);
 
 CREATE INDEX IF NOT EXISTS ix_entry_parent ON entries(session_id, parent_id);
 CREATE INDEX IF NOT EXISTS ix_entry_seq ON entries(session_id, seq, type);
@@ -39,7 +39,7 @@ CREATE TABLE IF NOT EXISTS scalar_values (
 	seq INTEGER NOT NULL,
 	value TEXT NOT NULL,
 	PRIMARY KEY (session_id, namespace, key)
-) WITHOUT ROWID;
+);
 
 CREATE TABLE IF NOT EXISTS list_values (
 	session_id TEXT NOT NULL,
@@ -48,7 +48,7 @@ CREATE TABLE IF NOT EXISTS list_values (
 	seq INTEGER NOT NULL,
 	value TEXT NOT NULL,
 	PRIMARY KEY (session_id, namespace, key, seq)
-) WITHOUT ROWID;
+);
 
 CREATE TABLE IF NOT EXISTS usage_ledger (
 	session_id TEXT NOT NULL,
@@ -59,7 +59,7 @@ CREATE TABLE IF NOT EXISTS usage_ledger (
 	usage TEXT NOT NULL,
 	details TEXT,
 	PRIMARY KEY (session_id, id)
-) WITHOUT ROWID;
+);
 
 CREATE INDEX IF NOT EXISTS ix_usage_seq ON usage_ledger(session_id, seq);
 
@@ -98,7 +98,7 @@ CREATE TABLE IF NOT EXISTS branch_entries (
 	entry_seq INTEGER NOT NULL,
 	entry_type TEXT NOT NULL,
 	PRIMARY KEY (session_id, branch_id, entry_id)
-) WITHOUT ROWID;
+);
 
 -- Ordered scans. entry_seq must follow session_id, branch_id directly or ORDER
 -- BY needs a temp b-tree; entry_id and entry_type trail so the index covers
@@ -116,6 +116,6 @@ CREATE TABLE IF NOT EXISTS branch_meta (
 	base_branch_id TEXT,
 	base_seq INTEGER,
 	PRIMARY KEY (session_id, branch_id)
-) WITHOUT ROWID;
+);
 
 CREATE UNIQUE INDEX IF NOT EXISTS ix_bm_tip ON branch_meta(session_id, tip_entry_id);
diff --git a/dist/sqlite/migrations.js b/dist/sqlite/migrations.js
index 4dd2494cab6123d61cbe56b0e551634662d7fa24..bdd7a770910b5a315a4e684397241d88b21ab757 100644
--- a/dist/sqlite/migrations.js
+++ b/dist/sqlite/migrations.js
@@ -1,7 +1,7 @@
 import { readFile } from "node:fs/promises";
 import { fileURLToPath } from "node:url";
 export async function applyInitialSchema(db) {
-    const migration = await readFile(fileURLToPath(new URL("./migrations/001_initial.sql", import.meta.url)), "utf8");
+    const migration = await readFile(fileURLToPath(new URL(/* @vite-ignore */ "./migrations/001_initial.sql", import.meta.url)), "utf8");
     db.exec(migration);
 }
 //# sourceMappingURL=migrations.js.map
