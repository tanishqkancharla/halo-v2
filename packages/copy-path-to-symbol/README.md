# Copy Path to Symbol

Select a symbol or place the cursor inside one, then run **Copy Path to Symbol** from the Command Palette or editor context menu.

The extension copies a workspace-relative reference such as:

```text
apps/electron/src/main/PluginService.ts#PluginService.load
```
