{
  "name": "halo-v2",
  "install": "pnpm install && node node_modules/electron/install.js",
  "repositoryDependencies": [
    "github.com/tanishqkancharla/tandem",
    "github.com/tanishqkancharla/maui"
  ],
  "terminals": [
    {
      "name": "halo-dev",
      "command": "mkdir -p /home/ubuntu/halo-workspace && HALO_USE_SWIFTSHADER=1 HALO_WORKSPACE_ROOT=/home/ubuntu/halo-workspace pnpm dev",
      "description": "Independent user server and Electron client; SwiftShader enabled for the headless renderer"
    },
    {
      "name": "agentation-mcp",
      "command": "AGENTATION_STORE=memory npx -y agentation-mcp server --port 4747",
      "description": "Agentation HTTP server so the dev toolbar can sync annotation notes"
    }
  ]
}
